# Model

ChatGPT 5.2

# Prompts

```markdown
# Role

Adopt the role of a senior frontend developer.

# Goal

Create a **web page with JavaScript logic that reverses the order of a string**.

Example: if I input `AI4Devs`, it should return `sveD4IA`.

# Rules

1. Adopt the role mentioned in #role.
2. Analyze the task mentioned in #goal.
3. Implement the task using the seed file `index.html` attached.

# Output

The output should be the modified `index.html` file and the new `script.js` file.
```
